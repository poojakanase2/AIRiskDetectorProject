pipeline {
    agent any
    environment {
        MY_VAR = "HelloWorld"
    }
    stages {
        stage('Example') {
            steps {
                echo "MY_VAR is ${env.MY_VAR}"
            }
        }
    }
}
