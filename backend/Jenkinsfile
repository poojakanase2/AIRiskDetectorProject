pipeline {
    agent any
    // Removed tools block as no Maven installation named 'Maven' is configured
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean install'
            }
        }
        // Preserve all other original stages, logic, and shell commands here
    }
}
