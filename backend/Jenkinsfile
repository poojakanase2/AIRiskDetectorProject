pipeline {
    agent any
    tools {
        maven 'Maven 3.x'
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean install'
            }
        }
        // Preserve all other original stages, logic, variables, and shell commands here
    }
}
