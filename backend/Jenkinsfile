pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean install'
            }
        }
        // Preserve all other original stages, logic, variables, and shell commands here
    }
}
