pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'make build'
            }
        }
        // Add other stages here, ensuring all steps are inside 'steps' blocks
    }
}