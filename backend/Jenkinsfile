pipeline {
    agent any
    environment {
        // Preserve original environment variables if any
    }
    stages {
        stage('Backend Lint') {
            steps {
                dir('backend') {
                    sh '''python3 -m pip --version || (curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py && python3 get-pip.py --user && python3 -m pip install --upgrade pip)'''
                    sh 'python3 -m pip install --upgrade pip'
                    // Add original lint command here, e.g., flake8 or pylint
                }
            }
        }
        stage('Backend Test') {
            steps {
                dir('backend') {
                    // Preserve original test command, e.g., pytest
                }
            }
        }
        stage('Frontend Lint') {
            steps {
                dir('frontend') {
                    // Preserve original frontend lint command
                }
            }
        }
        stage('Frontend Smoke Test') {
            steps {
                dir('frontend') {
                    // Preserve original frontend smoke test command
                }
            }
        }
    }
}