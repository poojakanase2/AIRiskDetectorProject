pipeline {
    agent any
    tools {
        maven 'maven' // Use the default Maven tool name configured in Jenkins
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean install'
            }
        }
        // ... (preserve all other original stages, logic, and shell commands exactly as in the original Jenkinsfile)
    }
}
