pipeline {
    agent any
    tools {
        maven 'Maven 3.8.1' // Replace with the Maven version name configured in Jenkins Global Tool Configuration
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean install'
            }
        }
        // ... (preserve all other original stages, logic, and shell commands exactly as in the original Jenkinsfile)
    }
}
