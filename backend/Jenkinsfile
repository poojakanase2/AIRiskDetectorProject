pipeline {
    agent any

    environment {
        IMAGE_NAME = "demo-app"
        TAG = "latest"
    }

    stages {
        stage('Checkout') {
            steps {
                echo "Fetching code from GitHub..."
            }
        }
    }
}
