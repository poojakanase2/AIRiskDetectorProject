pipeline {
    agent any
    tools {
        maven 'Maven' // Use the default Maven tool name configured in Jenkins Global Tools
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean install'
            }
        }
        // Preserve all other original stages, logic, and shell commands here
    }
}
